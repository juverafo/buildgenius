{% extends 'backoffice.html.twig' %}

{% block title %}Création d'un produit{% endblock %} {# Titre de la page #}

{% block body %} {# Contenu principal de la page #}
	<a href="{{path('admin_product')}}" class="btn btn-success mb-4">Les produits</a>
	<div>
		{{ form_start(form) }}
			{{form_row(form.name)}}
			{{form_row(form.reseller)}}
			{{form_row(form.link)}}
			{{form_row(form.price)}}
			{{form_row(form.description)}}
			{{form_row(form.category)}}
			{# {{form_row(form.img)}} #}
		{{ form_end(form) }} {# Affichage du formulaire de produits #}
	</div>
{% endblock %}